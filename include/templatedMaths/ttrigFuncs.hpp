#pragma once
#include "../macros.hpp"


// Returns the Cosine function
// of a number
template<typename Number>
Number cos(Number theta){
  return Number(0.0);
};



// Returns the Sine function
// of a number
template<typename Number>
Number sin(Number theta){
  return Number(0.0);
};

template<typename Number>
Number tan(Number theta){
  return Number(0.0);
};

template<typename Number>
Number acos(Number theta){
  return Number(0.0);
};

template<typename Number>
Number asin(Number theta){
  return Number(0.0);
};

template<typename Number>
Number atan(Number theta){
  return Number(0.0);
};

template<typename Number>
Number atan2(Number theta){
  return Number(0.0);
};
